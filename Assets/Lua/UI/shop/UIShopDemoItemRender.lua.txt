local Render = UI.CreateRenderTable() 
Render.chooseBeC = nil
function Render:SetUICompent(child)
-- print("InfoRender SetUICompent  ****  child.name  == " ,child.name )
	-- if child.name == "name" then
	-- 	self.name = child:GetComponent("Text") 
	-- elseif child.name == "Pic" then
	-- 	self.Pic = child:GetComponent("Image")
	-- elseif child.name == "level" then
	-- 	self.level = child:GetComponent("Text")  
	-- elseif child.name == "zbplayer" then
	-- 	self.zbplayer = child:GetComponent("Text")  
	 if child.name == "beChoose" then 
		self.choose = child:GetComponent("Button") 
		self.choose.onClick:RemoveAllListeners()  
		self.choose.onClick:AddListener(function() self:chooseB() end) 
	 elseif child.name == "buyNum" then
		self.buyNum = child:GetComponent("Text") 
	end
end

function Render:SetData(v) 
   -- print("InfoRender  SetData  1111111111"   )
	self.ShopData = v 
	self:RefreshShopItem()
end

function Render:RefreshShopItem() 
	 self.buyNum.text = "剩余购买：" ..self.ShopData.num
	--  self.level.text = "LV." .. self.EquipData.level    
	--  self.Pic.sprite = Public.ResMeg.GetSpritePath(self.EquipData.metaID,"equip")
	--  if self.EquipData.crossHeroID == "-1" then 
	-- 	self.zbplayer.text = ""
	--  else
 	-- 	if self.EquipData.crossHeroID == "S0" then
	-- 		self.zbplayer.text = "(装备于"..ConfigDiZiData.getVarByCustom("player_id", self.EquipData.crossHeroID).player_name .. ")"
	-- 	else
	-- 		self.zbplayer.text = "(装备于"..GameMainData.PetModel:getPetById(self.EquipData.crossHeroID).name.. ")"
	-- 	end                                  
	--  end
	--  if self.EquipData.equipID == GameMainData.equipChooseId then
	-- 	self.beChoose.sprite = Public.ResMeg.GetSpritePath("panel10","Public")
	-- 	self.chooseBeC = self.beChoose
	--  else
	-- 	self.beChoose.sprite = Public.ResMeg.GetSpritePath("panelmain2","Public")
	--  end 
	 
end







function Render:chooseB()  
	-- if self.chooseBeC ~= null then
	-- 	self.beChoose.sprite = Public.ResMeg.GetSpritePath("panel7","Public")
	-- end
	-- self.beChoose.sprite = Public.ResMeg.GetSpritePath("panel10","Public")
	-- self.chooseBeC = self.beChoose
	-- GameMainData.equipChooseId = self.EquipData.equipID
	Event.Call("chooseShop",self.ShopData.ID)
end




return Render